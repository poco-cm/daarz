# Institutional snapshot — Formation phase

**Taken:** 2026-08-09  
**Order:** 2, phase 4  
**Activated institutions:** 3 of 17

This snapshot was generated by reading the source of the activated institutions, not written by hand. It records what each institution could actually do at the moment Formation closed.

## `factory/`

**Authority:** Sole creator of agents. May build and register; may not run or stop them.  
**Activated in:** Order 2 phase 2 (Birth)

| Module | Lines | Public surface |
|---|---|---|
| `factory/runtime/factory.py` | 152 | `ConstructionError` — no public methods; `BuiltAgent` — agent_id, run; `AgentFactory` — build, create_and_register, rebuild_registered |
| `factory/schemas/agent_spec.py` | 124 | `SpecificationError` — no public methods; `AgentSpec` — to_dict, from_dict |
| `factory/services/handler.py` | 71 | `FactoryService` — no public methods |

Total: 347 lines of implementation.

## `memory/`

**Authority:** Durable record of what agents perceived and concluded. May read and write; may not act.  
**Activated in:** Order 2 phase 4 (Formation)

| Module | Lines | Public surface |
|---|---|---|
| `memory/runtime/memory.py` | 147 | `MemoryError_` — no public methods; `MemoryRecord` — no public methods; `InstitutionalMemory` — remember, recall, history, subjects, write_count; `MemoryWriter` — remember_run_end |
| `memory/services/handler.py` | 73 | `MemoryService` — no public methods |

Total: 220 lines of implementation.

## `safety/`

**Authority:** Holds the stop authority and the boundary limits. May halt any execution.  
**Activated in:** Order 2 phase 1

| Module | Lines | Public surface |
|---|---|---|
| `safety/boundaries/boundaries.py` | 96 | `BoundaryViolation` — no public methods; `Boundaries` — no public methods; `BoundaryEnforcer` — on_violation, start, operations, elapsed, remaining_operations, record_operation |
| `safety/kill_switch/kill_switch.py` | 111 | `ExecutionHalted` — no public methods; `SwitchState` — no public methods; `KillSwitch` — read, is_engaged, assert_clear, engage, release |
| `safety/services/handler.py` | 60 | `SafetyService` — no public methods |

Total: 267 lines of implementation.

## Dormant

The remaining 14 institutions named in §1 have complete directory structures and manifests but no running code and no protocol address: `archive`, `civilization`, `constitution`, `defense`, `economy`, `governance`, `institutions`, `knowledge`, `learning`, `population`, `research`, `scientists`, `security`, `universities`.

Addressing any of them over `protocols/institution/requests/` raises `ActivationError` at message construction. This is verified by `tests/integration/test_formation.py::test_a_dormant_institution_has_no_address`.

## Protocol traffic possible at this snapshot

| Action | Served by | Constitutional note |
|---|---|---|
| `remember` | `memory` | — |
| `recall` | `memory` | — |
| `build_agent` | `factory` | Refused when `authorised_by` names an agent (Article II) |
| `halt` | `safety` | Any activated institution may request it; release has no protocol action at all (Article I) |
